# Static Site

A minimal static site consisting of a single `index.html` file. No build step, framework, or dependencies are involved.

## Running locally

Open `index.html` directly in a browser, or serve it with any static file server, for example:

```bash
npx serve .
```

Or with the Netlify CLI:

```bash
netlify dev
```
